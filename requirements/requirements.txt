# Python standard library imports
python-dateutil==2.9.0.post0
requests>=2.32.0
httpx==0.27.2

# third party imports
## django and misc
channels==4.2.0
dj_database_url==2.1.0
django==5.2.16
django-filter==23.5
django-htmx==1.17.3
django-two-factor-auth==1.16.0
django-simple-captcha==0.6.0
django-citext==1.0.0
django-auto-logout==0.5.1
docutils==0.20.1
geopandas==1.1.2
markdown==3.10.1
pandas==2.3.3
openpyxl==3.1.5
psycopg2-binary==2.9.9
whitenoise==6.6.0
python-dotenv==1.2.2
azure-identity==1.17.1

# We don't use this but django-two-factor-auth requires it
# They are working to make it optional https://github.com/jazzband/django-two-factor-auth/issues/469
phonenumbers==8.13.31

## graphing
plotly==5.19.0

# NHS number
nhs-number==1.3.6

# profiling
django-silk==5.4.3

# live application server
gunicorn>=22.0.0

# Celery (background tasks)
celery==5.4.0
flower==2.0.1
redis==5.2.1

# Automatically reloading Celery on file changes
watchdog==6.0.0

# code linting and formatting
autopep8==2.0.4
djlint==1.36.3
ruff==0.16.1

# testing and code analysis
coverage==7.13.5
pytest-django==4.12.0
pytest-factoryboy==2.8.1
pytest-asyncio==1.3.0
freezegun==1.5.1
pytest==9.0.3

# versioning
bump2version

# Documentation site theme
# Ported from Material for MkDocs to Zensical (the successor, built by the same
# team). See https://zensical.org/docs/get-started/
zensical==0.0.59

# The following MkDocs plugins are NOT yet compatible with Zensical and are
# suspended pending Zensical's module system. Re-enable each when the
# corresponding Zensical backlog issue is resolved.
# mkdocs-git-committers-plugin-2==2.3.0            # https://github.com/zensical/backlog/issues/17
# mkdocs-git-revision-date-localized-plugin==1.2.4 # https://github.com/zensical/backlog/issues/18
# mkdocs-macros-plugin==1.0.5                      # replaced by zensical.extensions.macros (built into Zensical >=0.0.40)
# mkdocs-with-pdf==0.9.3                          # https://github.com/zensical/backlog/issues/25

# for colored logs
colorlog==6.8.2

# Vscode debugger
debugpy==1.8.8
